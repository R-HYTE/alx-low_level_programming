#include <stdio.h>
/**
 * main - Prints the lowercase alphabet in reverse
 * Return: 0
 */
int main(void)
{
	char myChar;

	myChar = 'z';
	while
		(myChar >= 'a')
		{
			putchar(myChar);
			myChar--;
		}
	putchar('\n');
	return (0);
}
